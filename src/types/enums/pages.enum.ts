export enum AppRoutes {
    LOGIN = "/login",
    REGISTER = "/register",
    DASHBOARD = "/",
    USER_LIST = "/users",
    USER = "/users/",
    CHAT = "/users/chat/",
    NEW_POST = "/new-post",
    CONFIG_PROFILE = "/profile-config"
}