export enum AppRoutes {
    LOGIN = "/login",
    REGISTER = "/register",
    DASHBOARD = "/",
    PROFILE = "/profile",
    NEW_POST = "/newPost",
    FRIENDS = "/friends",
    CHAT = "/friends/chat"
}